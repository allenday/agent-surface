# Bundled skill instructions

These instructions extend the repository-root and package `AGENTS.md` files.

- Keep `SKILL.md`, `reference.md`, and sidecars mutually consistent with shipped behavior.
- Preserve YAML-first, adaptive flow, no-ellipsis, bounded-output, and HATEOAS guidance.
- Test package-resource access from source and from the built wheel whenever sidecars change.
- Keep the skill concise; put extended examples and contracts in its referenced sidecars.
