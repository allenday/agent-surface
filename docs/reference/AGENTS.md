# Reference instructions

These instructions extend `docs/AGENTS.md` for `docs/reference/`.

- Document shipped behavior precisely and distinguish wire names from Python API names.
- Keep entries navigable by symbol, operation, and transport; examples illustrate rather than narrate.
- Verify every schema, default, error code, and command against code or tests before publishing it.
- Link outward to a tutorial or how-to for task-oriented guidance.
