# Concept instructions

These instructions extend `docs/AGENTS.md` for `docs/concepts/`.

- Explain the invariant and its trade-offs in plain language before relying on a term of art.
- Use a small concrete example, then link to an executable tutorial for the full workflow.
- Keep concepts transport-neutral unless the distinction itself is the subject.
- Do not turn concepts into configuration references or historical plans.
